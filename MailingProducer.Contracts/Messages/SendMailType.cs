namespace MailingProducer.Contracts.Messages
{
    /// <summary>
    /// Enum representing type of send mail
    /// </summary>
    public enum SendMailType
    {
        Console = 0, //only show messege in console 
        Server = 1   //send via mail server
    }
}
